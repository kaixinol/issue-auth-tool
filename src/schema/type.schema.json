{
    "type": "object",
    "properties": {
        "type": {
            "type": "string",
            "enum": [
                "del",
                "outdate",
                "alias",
                "invalid"
            ]
        },
        "reason": {
            "type": "string"
        }
    },
    "required": [
        "type"
    ],
    "if": {
        "properties": {
            "type": {
                "const": "invalid"
            }
        }
    },
    "then": {
        "required": [
            "reason"
        ]
    }
}